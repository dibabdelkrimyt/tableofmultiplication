#include <stdio.h>
#include <stdlib.h> 

void Prime_Number ( int a) {
int b; 
if ((a%b!=0) && (b!=a)&& (b!=1))
printf (" %d is a prime number",a); 
else 
printf (" %d is not a prime number",a);
}

int main () {
int n; 
printf("Give a: "); 
scanf ("%d", &n); 
Prime_Number (n); 
return 0;
} 
